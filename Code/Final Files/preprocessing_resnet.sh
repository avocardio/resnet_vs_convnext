#!/bin/bash

# create directory?

# install/download packages required for preprocessing steps (if not done via environment file)

# preprocessing steps for resnet
echo "  preprocessing resnet" 
python scripts/preprocessing.py --resnet